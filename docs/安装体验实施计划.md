# 一键优先安装体验：范围与决策

基线：`5906a859e46583b6440aa58514a83563a36b7d04`，原工作树 `agent/readme-entry-page` 干净，与其 upstream ahead/behind = 0/0。独立工作树使用 `agent/one-click-install-ux`，未配置 upstream。基线规则验证和 9 项单元测试通过。

## 目标与约束

用户先选客户端，再安装模块或明确标注的示例；失败后复制地址，最后才手动配置。保留 5 accepted / 5 pending / 8 rejected、既有来源锁、Apple 资格结论、私有配置边界、现有离线示例和全部权限。只改公开项目的安装 UX，不研究新域名、不改 proxy-rulesets 生产路由，不发布。

## 最小任务

| 问题 | 修改 / 预期收益 | 风险与依赖 | 文件 | 验证 | 决策 / 并行 |
| --- | --- | --- | --- | --- | --- |
| 手填规则是第一步 | 从既有 canonical 生成纯 Rule module，免逐条输入 | Shadowrocket 协议仅社区依据；模块可能覆盖主配置同域规则 | scripts/rules.py、modules | 五域集合/顺序/哈希、非 Rule 段拒绝、漂移测试 | APPROVE FOR IMPLEMENTATION；依赖协议研究 |
| 原示例依赖本地 provider | 新增自包含无节点示例，保留旧例 | ClashX 自动启用；Verge 首次启用；CTA 必须说明 REJECT/DIRECT | examples/clash-remote.yaml、生成器 | 内核隔离解析、Verge键要求、域一致 | APPROVE FOR IMPLEMENTATION；与文档可独立准备 |
| GitHub 清洗 custom scheme | 项目静态安装页，固定目标、资源核对、复制备用 | Pages 未开通；真实 App 未测 | install | scheme 解码、404/哈希不一致保护、复制失败备用、浏览器布局 | APPROVE FOR IMPLEMENTATION；不部署 |
| 首屏工程与手动内容太多 | 客户端入口优先、资格与失败入口紧随、研究移 docs | 未发布入口不得伪称可用 | README、教程、能力/验证记录 | 链接/锚点检查、独立小白 UX review | APPROVE FOR IMPLEMENTATION |
| HTTPS 一键闭环不在线 | 准备纯静态发布文件；待批准开通 Pages、发布后复测 | 需要仓库 setting / 发布授权 | 无权限文件修改 | 远程200、内容哈希、真机验收 | HUMAN DECISION REQUIRED；本次不执行 |

## 验收与独立审查

本地验收：新增格式均来自唯一源；现有源数据逐字不变；安装链接正确编码；无节点/订阅/MITM/DNS/FINAL进入模块；文案准确描述导入/激活；权限 diff 为 NONE；全部相关测试通过；独立技术和 UX reviewer 各 0 BLOCKER / 0 MAJOR。

线上验收单列：raw 和 Pages 稳定地址已发布且可达，真实客户端导入和匹配通过。本地 PASS 不等于线上验收通过。

方案独立 review 结论：允许本地实施，要求明确未上线、Shadowrocket 社区证据、Clash 自动激活副作用和发布权限边界。没有自行扩大范围。
