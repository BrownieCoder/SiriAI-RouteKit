# 验证记录

日期：2026-09-22。规则源锁定提交仍为 `760db6bb7adec25ec5eab38359536fbdcdf7eb29`，输入 SHA256 见 [source.lock.json](../source.lock.json)。本轮只改变安装 UX，没有同步规则源。

## 首次本地验证（2026-09-22）

| 检查 | 结果 | 能证明与不能证明 |
| --- | --- | --- |
| `python3 scripts/rules.py validate` | 通过 | 5 个精确域、来源、审计和全部生成物一致；不证明上游远程最新 |
| `python3 -m unittest discover -s tests` | 17 项通过 | 含原 9 项来源测试、新格式域集合/顺序、生成哈希、篡改拒绝、组合匹配样本、文档链接/锚点、无第三方代码 |
| `node --test tests/install.test.cjs` | 10 项通过 | 执行真实安装页脚本，验证三种 scheme 编码、显示/复制目标一致、404/离线/哈希不符不唤起、剪贴板失败、浏览器拒绝自动打开的文案、其他 GUI 仅复制 |
| `python3 scripts/rules.py generate` 后验证 | 通过 | 确定性生成；模块和新例没有手写另一套域名 |
| Mihomo v1.19.31 `-t` | 旧离线示例和新自包含远程示例均退出 0 | 使用新建临时 HomeDir，只复制公开 provider 和两个示例；没有加载用户配置或启动代理监听 |
| 桌面浏览器 | 安装首页显示并按客户端进入模块页 | 本地页面渲染和导航，不是真实客户端导入 |
| 390×844 窄屏浏览器 | Shadowrocket 与 ClashX 页面可读，风险说明在按钮前；资源不可用时按钮禁用 | 完整地址常显、可复制；不是 iOS / Safari 真机测试 |
| 浏览器复制反馈 | 成功复制有反馈；资源不可用时不会引导立即导入 | 剪贴板 API 不可用的降级由 Node 回归覆盖 |
| 原文件差异检查 | canonical、来源、18 项审计、source.lock、设备资格文档、技术原理和旧生成物未改 | 未重新研究 Apple 资格，不声称改变资格结论 |
| `git diff --check` | 通过 | 无空白错误 |
| CI permissions | 仍为 `contents: read` | 仅增加测试步骤和新生成物检查，无 Pages workflow 或部署权限 |

Mihomo 使用前次已从官方 release 下载的 v1.19.31 ARM64 二进制，本轮再次以 `-v` 核对版本。前次 gzip SHA256：`d131f44b3deb2a8356f7ac75048ad67a10d53243323951c4f3cda7b672922963`。二进制和临时配置不进入 Git。

组合匹配样本只证明给定顺序下精确规则先于宽泛 Apple/CDN 及兜底；不能代替 Shadowrocket 内部模块优先级、未知私人安全规则或 Mihomo UDP 跳过不兼容代理的真实验证。

## 首次线上观察与未完成验收（2026-09-22 历史状态）

- GitHub API：公开仓库 `BrownieCoder/SiriAI-RouteKit`、默认 `main`、Pages 尚未启用。
- 现有离线示例 raw：HTTP 200。
- 本轮新增模块、新远程示例、三个客户端 Pages 地址：HTTP 404（尚未发布）。README 如实保留文件安装入口，不使用不可达的一键按钮。
- 官方 GitHub Markdown API：HTTPS 对照链接保留，Shadowrocket scheme Markdown/HTML 链接被清洗；两个渲染模式相同。[研究记录与来源](客户端导入能力.md#github-为什么需要-https-安装页)。
- Shadowrocket / ClashX.Meta / Clash Verge Rev 真机协议唤起、安装、实际匹配及 Apple 功能均未实测。

线上闭环须另行授权发布后再验证，不能标成已完成。安装页 URL、raw URL、scheme 和实际客户端行为分别验收。

## 独立审查

- Reviewer A 技术审查：本地实现 0 BLOCKER / 0 MAJOR；来源范围、格式、导入影响和权限检查通过。修复了未核实的文件导入菜单表述，采用整块新建模块备用路径，并同步维护文档和验证记录。
- Reviewer B 小白 UX：本地 PASS，0 BLOCKER / 0 MAJOR。README 能按客户端定位入口、说明节点/配置影响、提供失败退路，并快速回答国行资格和排查起点。修复复制失效地址后错误引导，以及 Clash 示例的排障顺序。
- 上述结论是本地交付审查，不是线上发布验收，也不是真人 15 秒计时实验。

2026-09-22 首次本地实现阶段没有 push、merge、deploy、Pages setting 变更或私人配置读写。此前“尚未公开创建仓库”的历史描述不再适用于当前已公开的 repository；本轮新增资产尚未发布。


## Copilot 修复验证（2026-09-23）

修复 PR #2 的可变 URL 校验与二次下载不一致问题。安装脚本的 fetch、scheme、复制地址及四个 HTML 的无 JS 地址均固定到资源提交 `6bce8d5563cd13aef913035971decab4890fc267`。

- 模块和远程示例固定 URL：HTTP 200，SHA256 与生成清单逐项一致。
- 规则验证、17 项 Python、12 项 Node 测试及 git diff --check 全部通过。
- 新增回归覆盖校验后 main 改变、三客户端交接 URL 与校验请求相同、四个无 JS 地址锁定同一 SHA。
- 资源内容、域名、生成器及权限未改，复用此前内核解析与布局验证；未再唤起真实客户端。
- 固定版本不自动跟随 main 更新，维护文档已说明更新步骤；Pages 仍待发布，真机仍未测。
