# 验证记录

日期：2026-09-22。规则源提交：`760db6bb7adec25ec5eab38359536fbdcdf7eb29`。各文件 SHA256 见 [source.lock.json](../source.lock.json)。

| 检查 | 结果 | 边界 |
|---|---|---|
| `python3 scripts/rules.py validate` | 通过：5 个精确域、来源/审计/生成物/示例引用一致 | 仅本地锁定快照 |
| `python3 scripts/rules.py validate --source /公开源checkout` | 通过：指定本地源 HEAD 的三个文件与锁定源一致 | 未联网检查 GitHub 最新版本 |
| `python3 -m unittest discover -s tests` | 9 项通过 | 检查导入、未提交修改、来源标识、版本漂移、输出篡改、重复/通配与缺失来源记录 |
| 本地 Markdown 文件链接 | 通过 | 不等于外部链接永久可用 |
| Mihomo v1.19.31 `-t` | 官方内核在临时隔离 HomeDir 成功解析公开示例及 file/classical provider | 未启动用户代理配置、监听或真实 Apple 请求；未做联网转发测试 |
| Shadowrocket | 规则语法子集/组名/生成一致性静态检查通过 | 未在 Shadowrocket App 导入，未执行客户端“测试规则” |
| Apple 设备功能 | 未实测 | 不声称解锁成功、模型下载成功或等待名单已准入 |

Mihomo 二进制取自 MetaCubeX/mihomo 官方 v1.19.31 release；所下载 gzip 的 SHA256 为 `d131f44b3deb2a8356f7ac75048ad67a10d53243323951c4f3cda7b672922963`，未把二进制、临时日志或数据缓存放入本仓库。

本仓库没有 push、公开创建 repository、merge 或部署。CI 文件只是准备好的只读验证流程，尚未在 GitHub Actions 执行。真实客户端或网络验证需要按各客户端教程分别记录证据。
